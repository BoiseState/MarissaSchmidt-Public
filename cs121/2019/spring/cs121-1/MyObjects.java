/**
 * This example demonstrates how to create and use various objects.
 * 
 * @author marissa
 */
public class MyObjects
{
	public static void main(String[] args)
	{
		// Instantiate a String object
		
		// Instantiate a Scanner object (which methods do we know?)
		
		// Instantiate a Point object (show documentation, get, set)
		
		// Instantiate a Color object (show documentation)
		
		// Instantiate a MiniFig object (show documentation, example)
	}
}
